import React from "react";
import AppLayout from "../components/AppLayout";
import StubPage from "../components/StubPage";

export default function SettingsPartyPage() {
  return (
    <AppLayout>
      <StubPage title="Settings — Party" description="Party-related settings (Placeholder)" />
    </AppLayout>
  );
}
